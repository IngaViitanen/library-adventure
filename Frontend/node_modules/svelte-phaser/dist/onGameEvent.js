import { onMount } from 'svelte';
import { getGame } from './getGame';
export function onGameEvent(event, callback) {
    const game = getGame();
    onMount(() => {
        game.events.on(event, callback);
        return () => game.events.off(event, callback);
    });
}
//# sourceMappingURL=onGameEvent.js.map